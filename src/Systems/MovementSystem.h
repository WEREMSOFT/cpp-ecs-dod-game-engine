#pragma once

#include <glm/glm.hpp>

#include "../ECS/ECS.h"

class MovementSystem : public System
{
public:
	MovementSystem() {}
	void Update(double deltaTime)
	{
		// for (auto entity : GetEntities())
		// {
		// }
	}
};